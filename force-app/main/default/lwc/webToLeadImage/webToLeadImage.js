import { LightningElement } from 'lwc';

import IMAGES from '@salesforce/resourceUrl/Images';

export default class WebToLeadImage extends LightningElement {
    imageUrl = IMAGES + '/webToLead2cropped.png';
}
